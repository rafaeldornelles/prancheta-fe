import { InputmodeDirective } from './inputmode.directive';

describe('InputmodeDirective', () => {
  it('should create an instance', () => {
    const directive = new InputmodeDirective();
    expect(directive).toBeTruthy();
  });
});
